<div class="d-flex flex-column align-items-center justify-content-center text-center min-vh-100">

<div class="position-relative">
  <h2 class="mb-4">Scan QR code</h2>
  <div data-controller="qr-code-reader">
    <video data-qr-code-reader-target="video" width="390" height="400" data-qr-code-path=""></video>
  </div>

  <div class="mt-auto w-100 px-2" style="margin-bottom: 20px;">
    <a href="<%#= edit_return_path(@return) %>" class="btn btn-warning btn-sm">🤔 I don't have a QR code...</a>
  </div>
</div>


<script>
const qrCodePath = $('video[data-qr-code-reader-target="video"]').data('qr-code-path');
console.log(qrCodePath); // Use the qrCodePath as needed
</script>
